<ui:FluentWindow x:Class="SOPP.DiskUsageAnalysis"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SOPP"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="系统优化++ - 硬盘占用分析器" Height="490" Width="947" WindowStyle="None"
        Closing="FluentWindow_Closing">
    <Grid>
        <ui:TitleBar Title="系统优化++ - 硬盘占用分析器" Height="40" CanMaximize="False" ShowMaximize="False"/>
        <Grid Margin="10,40,10,10">
        <!-- 路径输入和控制区域 -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 路径输入 -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,5">
            <TextBox x:Name="PathTextBox" 
                     Width="400" 
                     Text="C:\" 
                     ToolTip="输入要扫描的目录路径"/>
            <Button x:Name="StartScanButton" 
                    Content="开始扫描" 
                    Click="StartScanButton_Click"
                    MinWidth="80"/>
            <Button x:Name="CancelScanButton" 
                    Content="取消" 
                    Click="CancelScanButton_Click"
                    MinWidth="80"
                    IsEnabled="False"/>
            </StackPanel>
            <ProgressBar x:Name="ScanProgressBar" 
                     Grid.Row="1" 
                     Height="10" 
                     Visibility="Collapsed"
                     Width="927"/>
        <TreeView x:Name="FileTreeViewControl" Grid.Row="2" Margin="0 5 0 0">
            <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <Grid Width="Auto" MinWidth="600">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" MinWidth="150"/>
                                <ColumnDefinition Width="Auto" MinWidth="100"/>
                                <ColumnDefinition Width="Auto" MinWidth="80"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" 
                           Text="{Binding Name}" 
                           TextTrimming="CharacterEllipsis"
                           MaxWidth="200"
                           VerticalAlignment="Center"
                           ToolTip="{Binding Name}"/>

                <!-- 文件大小（右对齐，避免挤压） -->
                        <TextBlock Grid.Column="1" 
                           Text="{Binding Size, Converter={local:FileSizeConverter}}" 
                           MinWidth="100" 
                           HorizontalAlignment="Right"
                           Margin="10 0 0 0"
                           VerticalAlignment="Center"/>

                <!-- 百分比（右对齐） -->
                        <TextBlock Grid.Column="2" 
                           Text="{Binding ParentPercentage, StringFormat={}{0:0.##}%}" 
                           MinWidth="80" 
                           HorizontalAlignment="Right"
                           Margin="10 0 0 0"
                           VerticalAlignment="Center"/>
                        </Grid>
                    </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
            <TreeView.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel/>
                </ItemsPanelTemplate>
            </TreeView.ItemsPanel>
        </TreeView>

            <StackPanel Grid.Row="3" Orientation="Vertical" Margin="0 5 0 0">
                <TextBlock x:Name="StatusTextBlock" FontSize="12" Foreground="Gray"/>
                <TextBlock x:Name="ErrorTextBlock" FontSize="12" Foreground="Red" Margin="0 2 0 0"/>
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
